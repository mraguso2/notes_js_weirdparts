function greet (name) {
    name = name || '<your name here>'; //setting default value
    console.log('Hello ' + name);
}

greet('testy');
greet();

//the OR operator is unique and can return values that coerce to true

